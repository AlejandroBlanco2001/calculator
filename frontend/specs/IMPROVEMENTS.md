# Improvements after first version

After finishg the first version I conduceted a manual test and I noticed the following:

- I need that when the user does the operation and have the result, when he adds another operator, it carries the result, for example `2+2 -> 4 (press +) -> 4 +`
- I need that the bigger the sentece goes, it should allow some type of horizontal scrolling
- It should have a carret, so I can clikc and move to that position (or with the keyboard)
- The errors should be red letters, and quite more small that the text used for the numbers